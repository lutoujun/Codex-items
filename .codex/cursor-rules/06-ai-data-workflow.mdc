---
description: 数据团队 AI 协作 SOP，供任务拆解、协作交接与提示词复用时参考
alwaysApply: false
---

# 数据团队 AI 协作 SOP

适用范围：数据报表设计、指标口径、数仓/大数据开发、SQL 与 Shell/Python 脚本、数据验证、上线验收、任务拆分与组员协作。

目标：把每次 AI 协作沉淀为可追溯的交付物，而不是一次性聊天结论。

## 1. 一条任务的标准闭环

```mermaid
flowchart LR
    A[任务卡: 目标和范围] --> B[口径和资产核实]
    B --> C{工作类型}
    C -->|报表/需求| D[PUD: PRD和指标设计]
    C -->|数仓/脚本| E[DWE: 模型和代码]
    C -->|数据核验| F[DVT: 断言和证据]
    D --> E
    E --> F
    F --> G[PAR: 验收与上线决策]
    G --> H[归档: 代码、SQL、证据、复盘]
```

任何任务先写任务卡；只有“答疑、解释、翻译”可不走完整闭环。

## 2. 任务卡：每次对话先提供的最小信息

复制以下模板发给 AI，未知项可明确写“待核实”，不要靠猜：

```markdown
【任务类型】报表设计 / 数仓开发 / 脚本开发 / 数据验证 / 上线验收 / 任务分配
【业务目标】一句话说明要支持的决策或要解决的问题
【范围】包含什么；明确不包含什么
【交付物】PRD / 脑图 / SQL / Shell或Python脚本 / 校验报告 / 验收清单
【数据资产】已知库表、脚本路径、报表链接、样例数据
【时间与粒度】统计日期、刷新频率、业务粒度
【指标口径】已有定义或待确认的问题
【约束】性能、历史回刷、上线窗口、兼容性、保留小数等
【验收标准】可量化的成功条件
```

## 3. 按工作类型调用 AI

| 场景 | 先让 AI 做什么 | 必须获得的成果 | 你需要确认的关键点 |
| --- | --- | --- | --- |
| 数据报表设计 | 梳理用户故事、维度、指标、筛选器和异常状态 | PRD、指标字典条目、页面/看板结构、验收场景 | 指标是否服务业务决策；默认时间、钻取和权限边界 |
| 指标/口径设计 | 明确公式、粒度、时间边界、去重和空值策略 | 指标定义表、示例 SQL、反例与边界清单 | 一个指标只有一个真值来源；跨月、退款、补发等边界 |
| 数仓/大数据开发 | 先查真实 schema 和已有链路，再做分层与表设计 | 建模说明、DDL、ETL/SQL、影响分析、回滚方案 | 粒度、主键、分区、增量逻辑、上游真实性 |
| 脚本开发 | 先阅读现有脚本与测试，再最小化修改 | 修改后的脚本、测试、运行方式、变更说明 | 幂等、异常处理、历史回刷、参数化与性能 |
| 数据验证 | 把口径转为可运行断言，区分源/目标和容差 | 校验 SQL、结果表、异常报告、根因假设 | 六维质量：完整性、一致性、准确性、及时性、唯一性、合规性 |
| 上线验收 | 对照 PRD、代码和验证报告生成验收项 | UAT 清单、缺陷分级、GO/NO-GO、回滚检查 | 无证据不通过；P0 和核心口径问题必须阻塞 |

## 4. 使用 MCP 查询数据的硬规则

1. 先说明需要验证的假设和预计输出，避免“先查再说”。
2. 每条 MCP SQL 在执行前必须写 `audit/mcp-sql/YYYY-MM-DD.jsonl` 的 `planned` 记录，并回读确认。
3. 执行后立即写入同一 `audit_id` 的终态记录；结论中引用审计文件和 ID。
4. 先 `SHOW COLUMNS` 或 `SHOW CREATE TABLE`，再写业务 SQL；禁止假定字段。
5. 结果异常时先做最小复现和分层对账，不直接改生产逻辑。

具体格式见 [MCP SQL 审计协议](../audit/mcp-sql/PROTOCOL.md)。

## 5. 推荐的协作节奏

### A. 设计阶段（PUD）

1. 业务先确认任务卡和指标字典。
2. AI 输出 PRD、看板结构、用户故事与验收条件。
3. 你确认“业务目标、口径、范围外事项”后，才进入开发。

### B. 开发阶段（DWE）

1. AI 读取真实 schema、已有脚本和血缘，列出影响范围。
2. 先提交建模/改造方案与测试点，再生成 SQL 或脚本。
3. 每个变更包含：输入、处理、输出、幂等策略、历史回刷策略、回滚方法。
4. 代码和 SQL 进入项目的 `src/`、`sql/`；说明进入 `docs/`。

### C. 验证阶段（DVT）

1. AI 将每个关键口径拆为断言：行数、主键、金额/数量、时间、枚举、边界样本。
2. 先验证最容易出错的场景：跨月、零值、空值、重复、退款/补发、历史补数、维度缺失。
3. 结论必须带实际数值、SQL 或审计 ID；不要接受“看起来正常”。
4. 证据保存到项目 `evidence/`，SQL 保存到项目 `sql/`。

### D. 验收阶段（PAR）

1. 以 PRD、指标口径和 DVT 报告为三份基准。
2. 输出唯一结论：`GO`、`GO-IF` 或 `NO-GO`。
3. P0/核心指标问题未关闭，不上线；同时确认回滚入口和责任人。

## 6. 给组员分配工作的方式

不要只分配“做一个报表/查一下问题”，而是分配可验收的任务包。

```markdown
【任务编号】DW-20260730-01
【负责人】姓名
【角色】PUD / DWE / DVT / PAR
【目标】完成什么业务结果
【输入】链接、表、脚本、口径、前置任务
【输出】精确文件路径和交付物名称
【验收】量化检查项；谁验收；何时验收
【边界】不做什么；是否可改线上逻辑
【风险】数据依赖、历史补数、权限、上线窗口
【截止时间】日期时间
```

建议按角色拆分而非按文件随意拆分：

- PUD：指标定义、报表需求、验收场景。
- DWE：模型、SQL、调度与脚本实现。
- DVT：独立复核口径、数据质量与回归。
- PAR/负责人：证据汇总、上线判定与问题闭环。

每个成员的交付必须有“产物链接 + 自测证据 + 未解决风险”三项；没有这三项，不进入下游。

## 7. 可直接复制的高质量提示词

### 7.1 报表/指标设计

```markdown
作为数据产品和数仓协作助手，基于以下业务目标设计报表。
先输出需要确认的口径问题；确认后再输出 PRD、指标定义表、维度/筛选器、异常状态、验收场景和数据链路。
不要假定库表或字段；未知项标记待核实。

[粘贴任务卡]
```

### 7.2 数仓或脚本改造

```markdown
作为 DWE，先阅读我提供的脚本/表结构/上下文，说明现状、影响范围和最小改造方案。
确认真实 schema 后再修改。交付：建模或逻辑说明、代码改动、测试、历史回刷和回滚方案。
所有口径变化必须列出上下游影响；不要编造字段或数据结果。

[粘贴任务卡和文件路径]
```

### 7.3 数据验证

```markdown
作为 DVT，为以下改动设计并执行回归验证。
先列出假设、真值来源和六维校验项；使用 MCP 前按 audit/mcp-sql/PROTOCOL.md 记录审计。
最终输出：校验项、SQL、实际值、结论、异常根因、审计文件与 audit_id。

[粘贴任务卡、改动说明和目标表]
```

### 7.4 任务拆分

```markdown
把以下目标拆成可并行、可验收的任务包。
按 PUD→DWE→DVT→PAR 的依赖关系排列；每项给出负责人角色、输入、输出文件、验收标准、风险和预计工期。
不要把需要同一份口径或同一张表的强依赖任务错误并行。

[粘贴目标]
```

## 8. 项目归档与复用

每个项目按以下目录留存，避免聊天结论丢失：

```text
projects/<project>/
├─ src/       # Python/Shell 等业务代码
├─ tests/     # 自动化测试
├─ sql/       # 可重跑 SQL 与 DDL
├─ docs/      # PRD、口径、设计和变更说明
├─ mindmaps/  # 脑图
└─ evidence/  # 查询结果、截图、验证报告
```

每次交付至少更新项目 `README.md`：当前目标、关键输入、产出路径、执行方式、最后验证日期、已知风险。

## 9. 负责人每周检查清单

- [ ] 新指标是否已进入唯一的指标字典，并有负责人？
- [ ] 本周所有 MCP 数据结论是否有审计 ID？
- [ ] 变更是否有测试、数据验证证据和回滚方案？
- [ ] 历史补数是否同时检查了时间范围、行数、主键和核心指标？
- [ ] 组员任务是否有明确输入、输出与验收人？
- [ ] 已完成产物是否已归入项目目录，而不是散落在聊天或根目录？
- [ ] 是否存在 P0/P1 数据问题或未关闭风险？
